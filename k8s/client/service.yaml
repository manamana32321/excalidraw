apiVersion: v1
kind: Service
metadata:
  name: excalidraw-client
  labels:
    app: excalidraw
    component: client
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: excalidraw
    component: client
